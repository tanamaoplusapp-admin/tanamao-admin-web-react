import React from 'react';
import { Image } from 'react-native';

const IconeChat = ({ style }) => (
  <Image source={require('./icone_chat.png')} style={style} resizeMode="contain" />
);

export default IconeChat;
