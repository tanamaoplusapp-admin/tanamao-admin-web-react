import React from 'react';
import { Image } from 'react-native';

const IconeEntrega = ({ style }) => (
  <Image source={require('./icone_entrega.png')} style={style} resizeMode="contain" />
);

export default IconeEntrega;
