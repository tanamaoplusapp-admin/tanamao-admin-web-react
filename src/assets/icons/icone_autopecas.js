import React from 'react';
import { Image } from 'react-native';

const IconeAutopecas = ({ style }) => (
  <Image source={require('./icone_autopecas.png')} style={style} resizeMode="contain" />
);

export default IconeAutopecas;
